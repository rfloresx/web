/* web_SockJsConnection.c
 *
 * This file contains the implementation for the generated interface.
 *
 * Don't mess with the begin and end tags, since these will ensure that modified
 * code in interface functions isn't replaced when code is re-generated.
 */

#include "web.h"

/* ::cortex::web::SockJsConnection::send(string message) */
cx_void web_SockJsConnection_send(web_SockJsConnection _this, cx_string message) {
/* $begin(::cortex::web::SockJsConnection::send) */
    CX_UNUSED(_this);
    CX_UNUSED(message);
/* $end */
}
