require 'rake/clean'

PACKAGE = 'evhtp'
NOCORTO = true

LIB_PUBLIC = [
  "evhtp"
]

INCLUDE = [
  "$(CORTO_INCLUDE)",
  "/usr/local/opt/openssl/include"
]

# Don't link with placeholder library generated by corto
LINK_PUBLIC = []

require "#{ENV['CORTO_BUILD']}/package"
