require 'rake/clean'

PACKAGE = 'evhtp'
NOCORTO = true

LIB = [
  "evhtp"
]

INCLUDE = [
  "$(CORTO_INCLUDE)",
  "$(CORTO_INCLUDE)/evhtp",
  "/usr/local/opt/openssl/include"
]

CFLAGS = []

require "#{ENV['CORTO_BUILD']}/package"
